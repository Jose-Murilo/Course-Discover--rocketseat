// Obejct

let person = {
    nome: "Murilo",
    age: 16,
    weight: 52.6,
    isAdmin: true
};

console.log(person);

// Obs: console.log(person.nome), o ponto "." é para chamar a propriedade do objeto e assim ele chama o valor da propriedade
console.log(person.nome);

console.log(`${person.nome} tem ${person.age} anos`) // person.nome é para pegar o objeto dentro de person com o valor nome