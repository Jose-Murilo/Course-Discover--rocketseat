const array = ["A", 2, "B"]

for (const iterator of array) {
    console.log(iterator);
}

// For of = Ele vai pegar salvar o nome em uma costante e vai interar ao array pegando o valor dele e guardando na variavel